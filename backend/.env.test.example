NODE_ENV=test
APP_ENV=test
PORT=3001

# Must point to a disposable database used only by automated tests
DATABASE_URL=
DIRECT_URL=

JWT_SECRET=

# Tests should use fakes by default; populate only for explicit integration tests
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=

CORS_ORIGIN=http://localhost:5173

